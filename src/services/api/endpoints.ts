/**
 * @description ENDPOINTS is an object that contains all the endpoints for the API
 */
const ENDPOINTS = {
  PROJECTS: {
    BASE: 'projects',
    STOP: 'stop',
    START: 'start',
  },
};

export default ENDPOINTS;
